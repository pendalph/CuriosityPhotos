import { StyleSheet } from 'react-native';
import { colors } from 'theme/colors';

export const styles = StyleSheet.create({
    root: {
        backgroundColor: colors.dark.PRIMARY_DARK,
        flex: 1
    }
});
